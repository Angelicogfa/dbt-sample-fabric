
# Name your project! Project names should contain only lowercase characters
# and underscores. A good package name should reflect your organization's
# name or the intended use of these models
name: 'treinamento_dbt'
version: '1.0.0'

# This setting configures which "profile" dbt uses for this project.
profile: 'treinamento_dbt'

# These configurations specify where dbt should look for different types of files.
# The `model-paths` config, for example, states that models in this project can be
# found in the "models/" directory. You probably won't need to change these!
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:         # directories to be removed by `dbt clean`
  - "target"
  - "dbt_packages"


# Configuring models
# Full documentation: https://docs.getdbt.com/docs/configuring-models

# Organização em camadas com diferentes estratégias de materialização e schemas
models:
  treinamento_dbt:
    +incremental_strategy: "delete+insert"
    # Camada de Staging - Ingestão 1:1 com as fontes
    staging:
      +materialized: view
      +schema: staging
      +tags: ["staging"]
    
    # Camada Intermediária - Transformações reutilizáveis
    intermediate:
      +materialized: view
      +schema: intermediate
      +tags: ["intermediate"]
    
    # Camada de Marts - Modelos finais para consumo
    marts:
      +materialized: table
      +schema: marts
      +tags: ["marts"]
      
      # Subdivisão por domínio de negócio
      # shared:
      #   +schema: marts_shared
      #   +tags: ["marts", "shared"]
      
      # core:
      #   +schema: marts_core
      #   +tags: ["marts", "core"]
      
      # finance:
      #   +schema: marts_finance
      #   +tags: ["marts", "finance"]
      
      # operations:
      #   +schema: marts_operations
      #   +tags: ["marts", "operations"]
      
      